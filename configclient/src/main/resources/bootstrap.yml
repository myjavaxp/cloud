spring:
  application:
    #对应server所获取的配置文件的{application}
    name: foo
  cloud:
    config:
      uri: http://localhost:8888/
      profile: test
      label: config-v2.0
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
management:
  security:
    enabled: false